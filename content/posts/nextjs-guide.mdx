---
title: Next.js App Router 시작하기
date: 2025-01-15
description: Next.js 14의 App Router를 활용한 풀스택 웹 애플리케이션 개발 가이드입니다.
tags:
  - nextjs
  - react
  - tutorial
published: true
---

# Next.js App Router 시작하기

Next.js의 App Router는 React Server Components를 기반으로 한 새로운 라우팅 시스템입니다.

## 프로젝트 구조

App Router에서는 `app` 디렉토리를 사용합니다:

```
app/
  layout.tsx
  page.tsx
  blog/
    [slug]/
      page.tsx
```

## 서버 컴포넌트

기본적으로 모든 컴포넌트는 서버 컴포넌트입니다.

```typescript
// 서버 컴포넌트 (기본값)
export default async function Page() {
  const data = await fetch("https://api.example.com/posts");
  const posts = await data.json();

  return (
    <ul>
      {posts.map((post) => (
        <li key={post.id}>{post.title}</li>
      ))}
    </ul>
  );
}
```

## 클라이언트 컴포넌트

상태나 브라우저 API가 필요하면 `"use client"` 지시어를 사용합니다.

```typescript
"use client";

import { useState } from "react";

export function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(count + 1)}>Count: {count}</button>;
}
```

## 데이터 페칭

서버 컴포넌트에서 직접 `async/await`로 데이터를 가져올 수 있습니다. 별도의 API 라우트가 필요 없습니다.

다음 포스트에서는 동적 라우팅과 메타데이터 설정을 다루겠습니다.
